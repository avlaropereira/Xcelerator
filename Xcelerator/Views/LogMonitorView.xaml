<UserControl x:Class="Xcelerator.Views.LogMonitorView"
         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
         xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
         xmlns:converters="clr-namespace:Xcelerator.Converters"
         mc:Ignorable="d" 
         d:DesignHeight="450" d:DesignWidth="800">
<UserControl.Resources>
    <converters:LogLineToColoredSegmentsConverter x:Key="LogLineToColoredSegmentsConverter"/>
    <converters:LogLineHighlightConverter x:Key="LogLineHighlightConverter"/>
    <converters:RefreshIntervalItemVisibilityConverter x:Key="RefreshIntervalItemVisibilityConverter"/>
    <converters:HighlightSelectedToVisibilityConverter x:Key="HighlightSelectedToVisibilityConverter"/>
    <converters:CountToVisibilityConverter x:Key="CountToVisibilityConverter"/>
    <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
    <converters:StringToVisibilityInvertedConverter x:Key="StringToVisibilityInvertedConverter"/>
    <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
</UserControl.Resources>
    <Grid x:Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*" MinHeight="150"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition x:Name="DetailPanelRow" Height="0" MinHeight="0" MaxHeight="{Binding ActualHeight, ElementName=LogLinesListBox, FallbackValue=350}"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition x:Name="HighlightPanelColumn" Width="0"/>
        </Grid.ColumnDefinitions>
        
        <!-- Status Bar -->
        <Border Grid.Row="0"
                Grid.Column="0"
                Background="#FFF5F5F5"
                BorderBrush="#FFE0E0E0"
                BorderThickness="0,0,0,1"
                Padding="10,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Loading Indicator -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <TextBlock Text="?" FontSize="14" Foreground="#FF0078D4" Margin="0,0,5,0">
                        <TextBlock.RenderTransform>
                            <RotateTransform x:Name="LoadingRotation" CenterX="7" CenterY="7"/>
                        </TextBlock.RenderTransform>
                        <TextBlock.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard RepeatBehavior="Forever">
                                        <DoubleAnimation Storyboard.TargetName="LoadingRotation"
                                                       Storyboard.TargetProperty="Angle"
                                                       From="0" To="360" Duration="0:0:2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </TextBlock.Triggers>
                    </TextBlock>
                    <TextBlock FontSize="12" Foreground="#FF666666" VerticalAlignment="Center"/>
                </StackPanel>
                
                <!-- Status Text -->
                <TextBlock Grid.Column="1"
                           Text="{Binding LogContent}"
                           FontSize="12"
                           Foreground="#FF2D2D30"
                           VerticalAlignment="Center"
                           Margin="10,0,0,0"/>
                
                <!-- Auto-Refresh Interval Dropdown -->
                <ComboBox Grid.Column="2"
                          Width="100"
                          Height="24"
                          FontSize="12"
                          VerticalAlignment="Center"
                          Margin="10,0,0,0"
                          HorizontalContentAlignment="Center"
                          VerticalContentAlignment="Center"
                          SelectedValue="{Binding RefreshIntervalMinutes, Mode=TwoWay}"
                          SelectedValuePath="Tag"
                          IsEnabled="{Binding IsRefreshDropdownEnabled}"
                          ToolTip="Auto-refresh interval (minutes)">
                    <ComboBox.ItemContainerStyle>
                        <Style TargetType="ComboBoxItem">
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            <Setter Property="Visibility">
                                <Setter.Value>
                                    <MultiBinding Converter="{StaticResource RefreshIntervalItemVisibilityConverter}">
                                        <Binding Path="Tag" RelativeSource="{RelativeSource Self}"/>
                                        <Binding Path="DataContext.LoadTimeSeconds" RelativeSource="{RelativeSource AncestorType=ComboBox}"/>
                                    </MultiBinding>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ComboBox.ItemContainerStyle>
                    <ComboBoxItem Content="Off" Tag="0" IsSelected="True"/>
                    <ComboBoxItem Content="1 min" Tag="1"/>
                    <ComboBoxItem Content="2 min" Tag="2"/>
                    <ComboBoxItem Content="3 min" Tag="3"/>
                    <ComboBoxItem Content="4 min" Tag="4"/>
                    <ComboBoxItem Content="5 min" Tag="5"/>
                    <ComboBoxItem Content="7 min" Tag="7"/>
                </ComboBox>

                <!-- Highlight Panel Toggle Button -->
                <Button Grid.Column="3"
                        Width="28"
                        Height="24"
                        FontSize="14"
                        FontWeight="Bold"
                        Background="Transparent"
                        BorderThickness="1"
                        BorderBrush="#FFCCCCCC"
                        Foreground="#FF0078D4"
                        Cursor="Hand"
                        ToolTip="Toggle highlight panel"
                        Margin="10,0,0,0"
                        Click="ToggleHighlightPanel_Click">
                    <TextBlock Text="?" FontFamily="Segoe MDL2 Assets" FontSize="14"/>
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="3">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                              VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#FFEEEEEE"/>
                                                <Setter Property="BorderBrush" Value="#FF0078D4"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter Property="Background" Value="#FFE1E1E1"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Border>
        
        <!-- Virtualized Log Lines with Syntax Highlighting -->
        <ListBox x:Name="LogLinesListBox"
                 Grid.Row="1"
                 Grid.Column="0"
                 ItemsSource="{Binding LogLines}"
                 SelectedItem="{Binding SelectedLogLine, Mode=TwoWay}"
                 VirtualizingPanel.IsVirtualizing="True"
                 VirtualizingPanel.VirtualizationMode="Recycling"
                 ScrollViewer.HorizontalScrollBarVisibility="Auto"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 BorderThickness="0"
                 Background="White"
                 Padding="10">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border x:Name="ItemBorder"
                                        Background="Transparent"
                                        Padding="4,2"
                                        Cursor="Hand">
                                    <ContentPresenter/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="ItemBorder" Property="Background" Value="#FFF5F5F5"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="ItemBorder" Property="Background" Value="#FFFFFF99"/>
                                        <Setter TargetName="ItemBorder" Property="BorderBrush" Value="#FFCCCC00"/>
                                        <Setter TargetName="ItemBorder" Property="BorderThickness" Value="2"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <ItemsControl>
                        <ItemsControl.ItemsSource>
                            <MultiBinding Converter="{StaticResource LogLineHighlightConverter}">
                                <Binding/>
                                <Binding Path="DataContext.SearchText" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                <Binding Path="DataContext.SelectedHighlight" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                            </MultiBinding>
                        </ItemsControl.ItemsSource>
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{Binding BackgroundBrush}" CornerRadius="2" Padding="0">
                                    <TextBlock Text="{Binding Text}"
                                               Foreground="{Binding Brush}"
                                               FontFamily="Consolas"
                                               FontSize="11"/>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        
        <!-- Empty State -->
        <TextBlock Grid.Row="1"
                   Grid.Column="0"
                   Text="No logs loaded yet. Loading..."
                   FontSize="14"
                   Foreground="#FF999999"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding LogLines.Count}" Value="0">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
        
        <!-- GridSplitter for resizing detail panel -->
        <GridSplitter Grid.Row="2"
                      Grid.Column="0"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Center"
                      Background="Transparent"
                      Cursor="SizeNS"
                      ResizeBehavior="PreviousAndNext"
                      ResizeDirection="Rows">
            <GridSplitter.Style>
                <Style TargetType="GridSplitter">
                    <Setter Property="Height" Value="5"/>
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsDetailPanelVisible}" Value="False">
                            <Setter Property="Height" Value="0"/>
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </GridSplitter.Style>
        </GridSplitter>
        
        <!-- Detail Panel -->
        <Border Grid.Row="3"
                Grid.Column="0">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Background" Value="#FFF8F9FA"/>
                    <Setter Property="BorderBrush" Value="#FF0078D4"/>
                    <Setter Property="BorderThickness" Value="0,2,0,0"/>
                    <Setter Property="Padding" Value="15"/>
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsDetailPanelVisible}" Value="False">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="0"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- Header -->
                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0"
                               Text="Selected Log Line Details"
                               FontSize="13"
                               FontWeight="SemiBold"
                               Foreground="#FF2D2D30"
                               VerticalAlignment="Center"/>
                    
                    <TextBlock Grid.Column="1"
                               FontSize="11"
                               Foreground="#FF666666"
                               VerticalAlignment="Center"
                               Margin="10,0,0,0"
                               Text="Click a line above to view its full content here"/>
                    
                    <!-- Close Button -->
                    <Button Grid.Column="2"
                            Content="?"
                            Width="24"
                            Height="24"
                            FontSize="14"
                            FontWeight="Bold"
                            Background="Transparent"
                            BorderThickness="1"
                            BorderBrush="#FFCCCCCC"
                            Foreground="#FF666666"
                            Cursor="Hand"
                            ToolTip="Close detail panel"
                            Click="CloseDetailPanel_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="3">
                                                <ContentPresenter HorizontalAlignment="Center"
                                                                  VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#FFEEEEEE"/>
                                                    <Setter Property="BorderBrush" Value="#FF999999"/>
                                                </Trigger>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter Property="Background" Value="#FFDDDDDD"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
                
                
                
                <!-- Content Area with ScrollViewer -->
                <Border Grid.Row="1"
                        Background="White"
                        BorderBrush="#FFE0E0E0"
                        BorderThickness="1"
                        CornerRadius="4">
                    <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                                  VerticalScrollBarVisibility="Auto">
                        <TextBox Text="{Binding SelectedLogLine, Mode=OneWay}"
                                 FontFamily="Consolas"
                                 FontSize="11"
                                 Foreground="#FF2D2D30"
                                 TextWrapping="Wrap"
                                 Padding="10"
                                 IsReadOnly="True"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 IsReadOnlyCaretVisible="True">
                            <TextBox.Resources>
                                <Style TargetType="{x:Type Border}">
                                    <Setter Property="CornerRadius" Value="0"/>
                                </Style>
                            </TextBox.Resources>
                        </TextBox>
                    </ScrollViewer>
                </Border>
            </Grid>
        </Border>

        <!-- GridSplitter for resizing highlight panel -->
        <GridSplitter Grid.Row="0"
                      Grid.RowSpan="4"
                      Grid.Column="1"
                      Width="5"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Stretch"
                      Background="Transparent"
                      Cursor="SizeWE"
                      ResizeBehavior="PreviousAndNext"
                      ResizeDirection="Columns">
            <GridSplitter.Style>
                <Style TargetType="GridSplitter">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsHighlightPanelVisible}" Value="False">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </GridSplitter.Style>
        </GridSplitter>

        <!-- Highlight Panel - Modern Card Style -->
        <Border Grid.Row="0"
                Grid.RowSpan="4"
                Grid.Column="2"
                Background="#FFFAFAFA"
                BorderBrush="#FFE0E0E0"
                BorderThickness="1,0,0,0"
                Padding="15">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsHighlightPanelVisible}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <Grid Grid.Row="0" Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               Text="Highlights"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Foreground="#FF2D2D30"
                               VerticalAlignment="Center"/>

                    <!-- Close Button -->
                    <Button Grid.Column="1"
                            Content="?"
                            Width="24"
                            Height="24"
                            FontSize="12"
                            FontWeight="Bold"
                            Background="Transparent"
                            BorderThickness="1"
                            BorderBrush="#FFCCCCCC"
                            Foreground="#FF666666"
                            Cursor="Hand"
                            ToolTip="Close highlight panel"
                            Click="CloseHighlightPanel_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="3">
                                                <ContentPresenter HorizontalAlignment="Center"
                                                                  VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#FFEEEEEE"/>
                                                    <Setter Property="BorderBrush" Value="#FF999999"/>
                                                </Trigger>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter Property="Background" Value="#FFDDDDDD"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>

                <!-- Search Box -->
                <StackPanel Grid.Row="1" Margin="0,0,0,15">
                    <TextBlock Text="Search Text"
                               FontSize="11"
                               FontWeight="SemiBold"
                               Foreground="#FF666666"
                               Margin="0,0,0,5"/>

                    <Border Background="White"
                            BorderBrush="#FFE0E0E0"
                            BorderThickness="1"
                            CornerRadius="4">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Search Icon -->
                            <TextBlock Grid.Column="0"
                                       Text="?"
                                       FontFamily="Segoe MDL2 Assets"
                                       FontSize="12"
                                       Foreground="#FF999999"
                                       VerticalAlignment="Center"
                                       Margin="8,0,5,0"/>

                            <!-- Search TextBox -->
                            <TextBox Grid.Column="1"
                                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                     BorderThickness="0"
                                     Background="Transparent"
                                     VerticalAlignment="Center"
                                     Padding="0,8"
                                     FontSize="12"
                                     Foreground="#FF2D2D30"/>

                            <!-- Clear Button -->
                            <Button Grid.Column="2"
                                    Content="?"
                                    Width="20"
                                    Height="20"
                                    FontSize="10"
                                    FontFamily="Segoe MDL2 Assets"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Foreground="#FF999999"
                                    Cursor="Hand"
                                    ToolTip="Clear search"
                                    Margin="0,0,5,0"
                                    Visibility="{Binding SearchText, Converter={StaticResource StringToVisibilityConverter}}"
                                    Click="ClearSearch_Click">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}"
                                                            CornerRadius="10">
                                                        <ContentPresenter HorizontalAlignment="Center"
                                                                          VerticalAlignment="Center"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#FFEEEEEE"/>
                                                            <Setter Property="Foreground" Value="#FF666666"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </Grid>
                    </Border>

                    <!-- Match Count -->
                    <TextBlock Margin="0,5,0,0"
                               FontSize="11"
                               Foreground="#FF666666"
                               Visibility="{Binding SearchText, Converter={StaticResource StringToVisibilityConverter}}">
                        <Run Text="{Binding MatchCount, Mode=OneWay}"/>
                        <Run Text=" matches found"/>
                    </TextBlock>
                </StackPanel>

                <!-- Color Swatches -->
                <ScrollViewer Grid.Row="2"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <ListBox ItemsSource="{Binding HighlightSettings}"
                             SelectedItem="{Binding SelectedHighlight, Mode=TwoWay}"
                             Background="Transparent"
                             BorderThickness="0"
                             Padding="0">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Vertical" />
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Margin" Value="0,0,0,6"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border x:Name="ItemBorder"
                                                    Background="White"
                                                    BorderBrush="#FFE0E0E0"
                                                    BorderThickness="1"
                                                    CornerRadius="4"
                                                    Padding="8,6"
                                                    Cursor="Hand">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>

                                                    <!-- Color Swatch -->
                                                    <Border Grid.Column="0"
                                                            x:Name="SwatchBorder"
                                                            Width="14"
                                                            Height="14"
                                                            Background="{Binding BackgroundBrush}"
                                                            BorderBrush="{Binding BorderBrush}"
                                                            BorderThickness="1"
                                                            CornerRadius="2"
                                                            VerticalAlignment="Center">
                                                        <Border.Effect>
                                                            <DropShadowEffect Color="#40000000" 
                                                                              BlurRadius="2" 
                                                                              ShadowDepth="1" 
                                                                              Opacity="0.2"/>
                                                        </Border.Effect>
                                                    </Border>

                                                    <!-- Label -->
                                                    <TextBlock Grid.Column="1"
                                                               Text="{Binding Name}"
                                                               FontSize="11"
                                                               Foreground="#FF2D2D30"
                                                               VerticalAlignment="Center"
                                                               Margin="8,0,0,0"/>

                                                    <!-- Selected Checkmark -->
                                                    <TextBlock Grid.Column="2"
                                                               Text="?"
                                                               FontFamily="Segoe MDL2 Assets"
                                                               FontSize="10"
                                                               Foreground="{Binding BorderBrush}"
                                                               VerticalAlignment="Center"
                                                               Visibility="{Binding IsSelected, Converter={StaticResource HighlightSelectedToVisibilityConverter}}"/>
                                                </Grid>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="ItemBorder" Property="Background" Value="#FFF5F5F5"/>
                                                    <Setter TargetName="ItemBorder" Property="BorderBrush" Value="#FFCCCCCC"/>
                                                </Trigger>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="ItemBorder" Property="BorderBrush" Value="#FF0078D4"/>
                                                    <Setter TargetName="ItemBorder" Property="BorderThickness" Value="2"/>
                                                    <Setter TargetName="SwatchBorder" Property="BorderThickness" Value="2"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>
